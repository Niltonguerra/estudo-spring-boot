spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/meudb
    username: postgres
    password: senha123

  jpa:
    hibernate:
      ddl-auto: update

#  flyway:
#    enabled: true
#    locations: classpath:db/migration

server:
  port: 8081

#logging:
#  level:
#    org.flywaydb: DEBUG